version: '2'
services:
    admin:
        build:
            context: .
            args:
                env: admin
    client:
        build:
            context: .
            args:
                env: client
