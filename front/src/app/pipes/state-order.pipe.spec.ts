import { StateOrderPipe } from './state-order.pipe';

describe('StateOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new StateOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
